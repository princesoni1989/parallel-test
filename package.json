{
  "name": "parallel-test",
  "version": "1.0.0",
  "description": "Example of running test cases in parallel and sequential manner.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --opts mocha.opts",
    "test-parallel": "mocha-parallel-tests --opts mocha.opts --max-parallel 3"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-preset-es2015": "^6.24.1",
    "expect.js": "^0.3.1",
    "mocha": "^3.5.3",
    "mocha-parallel-tests": "^1.2.9",
    "babel-cli": "^6.26.0",
    "should": "^13.1.0"
  },
  "keywords": [
    "test",
    "mocha",
    "mocha-parallel-test",
    "parallel-test"
  ]
}
